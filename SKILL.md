---
name: bnu-classroom
description: 查询北京师范大学空闲教室和课表占用；支持指定教学楼今明后三天、多个大节全部空闲筛选，以及指定教室七天课表。用户寻找自习教室或询问教室空闲时使用。
---

# 北师大空闲教室查询

使用本 skill 目录下的 `scripts/bnu_classroom.py`。把下文 `<script>` 替换为相对于当前 SKILL.md 解析得到的脚本绝对路径，命令中用引号包裹该路径；不要依赖当前工作目录或某台机器的安装路径。需要 Python 3.9+ 和到学校接口的网络连接。

## 查询

优先使用 `--json`。它输出已验证的数据，`--free-slots` 在 JSON 与文本模式下均生效。

```sh
python3 "<script>" --list-buildings --json
python3 "<script>" --building 教二楼 --day tomorrow --free-slots 5 6 --json
python3 "<script>" --building 教四楼 --room 101 --json
```

- `--building`：支持楼宇用 `--list-buildings` 获取。脚本默认教二楼，但不能把默认查询描述为全校结果。优先使用用户指定或上下文确定的楼宇；无楼宇信息时询问。用户要求所有支持楼宇时逐栋查询，汇总结果并指出失败的楼宇。
- `--day`：`today`、`tomorrow`、`2daysl` 分别为今天、明天、后天。楼宇查询默认今天，不支持任意日期。
- `--room`：返回指定楼宇中单间教室的七天课表，不能与 `--day` 同用。若用户指定日期，在返回的 `detail[].date` 中选择；返回范围外的日期不能推断。
- `--free-slots 3 4`：筛选第三、第四大节**全部空闲**的教室；单间模式则筛选满足条件的日期。
- 默认直连；环境要求代理时可使用 `--use-proxy`。不要无限重试，临时网络错误最多重试一次。

## 时间解释

使用当前北京时间（Asia/Shanghai）解析今天、明天、现在等表述。六个大节如下（不是六个小节）：

| 大节 | 时间 |
| --- | --- |
| 1 | 08:00–09:40 |
| 2 | 10:00–11:40 |
| 3 | 13:30–15:10 |
| 4 | 15:30–17:10 |
| 5 | 18:00–19:40 |
| 6 | 19:50–21:30 |

- “下午”默认查询 3、4，“晚上”默认查询 5、6；在答复中说明实际查询时段。
- “现在”仅选择当前所在大节；如果处于课间、午休或其他未覆盖时间，说明该时间没有对应数据，可提供相邻大节情况但不能声称当前空闲。
- 自定义时间段查询所有与之重叠的大节（按起点包含、终点不包含判断）。例如 14:00–16:00 检查 3、4，并说明 15:10–15:30 课间缺少数据，不能保证全程可用。
- 不把超出三天的楼宇请求悄悄改成今天，不根据时间段之外的结果推断空闲。

## 读取与答复

- 楼宇记录在 `result`，单间记录在 `detail`。每条 `status` 的六个整数对应六个大节，0 表示课表空闲，非 0 表示占用。
- `query_date` 是楼宇查询按北京时间计算的日期；单间以每条 `date` 为准。`fetched_at` 是抓取时间，不是学校数据更新时间。
- `count` 为筛选后条数，`total_count` 为筛选前条数。`total_count=0` 表示接口没有记录，不能断言整栋楼空闲或教室不存在；有原始记录但 `count=0` 才表示没有匹配记录。
- 成功退出码为 0；网络或数据异常退出码为 1，JSON 模式输出 `success: 0` 和 `error`；参数错误为退出码 2、错误写入 stderr。失败不能回答“没有空教室”。
- 回答列出楼宇、实际日期、空闲时段和教室；必要时附 `source_url` 和抓取时间。数据多时可分组摘要，不编造容量、设施或开放状态。
- 这是非官方工具，读取学校微信端课表接口。无课不等于无人、开门或允许自习，简要提醒以教务安排和现场情况为准。
